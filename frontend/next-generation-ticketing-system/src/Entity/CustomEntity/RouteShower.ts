class RouteShower {
  routeId: number;
  districts: string[];

  constructor(routeId: number, districts: string[]) {
    this.routeId = routeId;
    this.districts = districts;
  }
}

export default RouteShower;
